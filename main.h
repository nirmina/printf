#ifndef _MAIN_H
#define _MAIN_H
#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>
int handle_int(int num);
int _putchar(char c);
int handle_string(char *str);
int _printf(const char *format, ...);
#endif
